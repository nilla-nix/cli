import kleur from "kleur";

const message = `
  --help, -h                Show this help message
  --verbose, -v             Increase logging verbosity, up to 3 times
  --version                 Print the version of the Nilla CLI
`.substring(1).trimEnd();

export default message;
